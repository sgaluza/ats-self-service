# ATS Self-Service Platform

> Residential Proxy Service with Self-Service WireGuard VPN Management

## 📋 Обзор проекта

ATS Self-Service Platform — это полнофункциональная система управления residential proxy через WireGuard VPN туннели. Платформа позволяет пользователям устанавливать роутеры с WireGuard у себя дома или в офисе и получать доступ к своему резидентному IP-адресу через защищенный HTTP/SOCKS proxy из любой точки мира.

### Основная концепция

1. **Пользователь** устанавливает роутер с поддержкой WireGuard у себя дома/в офисе
2. **Через веб-интерфейс** создает конфигурацию WireGuard
3. **Система** устанавливает VPN-туннель между роутером пользователя и сервером
4. **Сервер** предоставляет HTTP/SOCKS proxy с авторизацией, который проксирует трафик через IP адрес пользователя
5. **Пользователь** может использовать этот proxy из любой точки мира, при этом внешний мир видит его домашний IP

### Применение

- ✅ Residential proxy для веб-скрейпинга и тестирования
- ✅ Удаленный доступ через свой домашний IP
- ✅ Обход гео-блокировок через свой IP
- ✅ Безопасный доступ к домашней/корпоративной сети

## 🏗️ Архитектура проекта

Проект построен как **monorepo** с использованием:
- **Turborepo** — система сборки и оркестрации
- **pnpm** — менеджер пакетов
- **TypeScript** — строгая типизация во всех пакетах

```
ats-self-service/
├── apps/
│   ├── web/              # Frontend приложение (React + Next.js)
│   └── api/              # Backend API (Node.js + Express/Fastify)
├── packages/
│   ├── ui/               # Переиспользуемые UI компоненты
│   ├── types/            # Общие TypeScript типы
│   ├── config/           # Общие конфигурации (ESLint, TypeScript, etc.)
│   └── utils/            # Общие утилиты
├── docker/               # Docker конфигурации для сервисов
├── docs/                 # Документация проекта
└── tests/                # E2E тесты
```

## 🚀 Технологический стек

### Frontend
- **React 18** — UI библиотека
- **Next.js 14+** — React фреймворк с App Router
- **TypeScript** — типизация
- **TailwindCSS** — стилизация
- **Storybook** — разработка и документирование компонентов
- **React Query** — управление серверным состоянием
- **Zustand** — управление клиентским состоянием
- **Vitest + React Testing Library** — тестирование

### Backend
- **Node.js 20+** — runtime
- **TypeScript** — типизация
- **Fastify** — HTTP framework
- **Supabase** — база данных (PostgreSQL) + Auth + Realtime
- **Prisma** — ORM
- **Zod** — валидация данных
- **Vitest** — тестирование
- **WireGuard** — VPN туннели
- **HTTP Proxy** — прокси-сервер с аутентификацией

### DevOps & Infrastructure
- **Docker & Docker Compose** — контейнеризация
- **pnpm** — package manager
- **Turborepo** — monorepo orchestration
- **GitHub Actions** — CI/CD
- **ESLint + Prettier** — линтинг и форматирование

## 📦 Основные функции

### Для пользователей

#### 🔐 Регистрация и аутентификация
- Регистрация только по приглашениям
- OAuth (GitHub, Google)
- Управление паролями и восстановление
- Безопасное хранение credentials

#### 🔧 Управление WireGuard подключениями
- Генерация конфигурации WireGuard (ключи, IP, порт)
- Пошаговый гайд по настройке роутера (текст + картинки/видео)
- Автоматическое ожидание и проверка соединения
- Тестирование скорости и доступности интернета
- Просмотр настроек proxy (username, password, IP, port)

#### 📊 Мониторинг и статистика
- Статус подключений в реальном времени
- Объем трафика (daily, weekly, monthly, custom period)
- История подключений
- Скорость соединения
- Геолокация и IP-адрес
- Повторная проверка соединения по запросу

#### ⚙️ Управление настройками
- Редактирование proxy credentials (username/password)
- Регенерация Pre-Shared Key (PSK)
- Управление несколькими подключениями
- Удаление подключений с подтверждением

### Для администраторов

#### 👥 Управление пользователями
- Просмотр всех зарегистрированных пользователей
- Поиск и фильтрация пользователей
- Блокировка/разблокировка пользователей
- Удаление пользователей
- Сброс паролей
- Имперсонация пользователей (вход от имени)

#### 📨 Управление приглашениями
- Создание новых приглашений
- Отправка приглашений на email
- Повторная отправка приглашений
- Просмотр истории приглашений
- Отзыв и деактивация приглашений
- Массовая генерация приглашений
- Статус доставки email
- Копирование пригласительных ссылок
- Статистика использования приглашений

#### 📈 Мониторинг и аналитика
- Количество коннекторов на пользователя
- Статистика трафика по пользователям
- Мониторинг работоспособности соединений (uptime, disconnects)
- QoS метрики (скорость, latency)
- Графики трафика (день, неделя, месяц, произвольный период)
- Детальная статистика по каждому подключению
- Real-time обновления данных

#### 🔧 Управление подключениями
- Просмотр настроек подключений пользователей
- Редактирование настроек от имени пользователя
- История изменений
- Предупреждения при изменении критичных параметров

## 🛠️ Установка и запуск

### Требования

- **Node.js** >= 20.0.0
- **pnpm** >= 9.0.0
- **Docker** >= 24.0.0
- **Docker Compose** >= 2.20.0

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/your-org/ats-self-service.git
cd ats-self-service

# Установка зависимостей
pnpm install

# Копирование environment файлов
cp apps/web/.env.example apps/web/.env.local
cp apps/api/.env.example apps/api/.env
```

### Настройка окружения

#### Frontend (`apps/web/.env.local`)
```env
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
```

#### Backend (`apps/api/.env`)
```env
NODE_ENV=development
PORT=3001
DATABASE_URL=postgresql://user:password@localhost:5432/ats
SUPABASE_URL=your-supabase-url
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
JWT_SECRET=your-jwt-secret
```

### Запуск в режиме разработки

```bash
# Запуск всех сервисов (frontend + backend)
pnpm dev

# Или запуск отдельно:
pnpm --filter @ats/web dev          # Frontend на http://localhost:3000
pnpm --filter @ats/api dev          # Backend на http://localhost:3001
```

### Запуск Storybook

```bash
pnpm storybook
# Откроется на http://localhost:6006
```

### Запуск с Docker

```bash
# Запуск всех сервисов (PostgreSQL, Redis, WireGuard, API)
pnpm docker:up

# Просмотр логов
pnpm docker:logs

# Остановка
pnpm docker:down
```

## 🧪 Тестирование

Проект имеет **100% покрытие тестами** как для frontend, так и для backend.

```bash
# Запуск всех тестов
pnpm test

# Запуск тестов с покрытием
pnpm test:coverage

# Запуск тестов для конкретного пакета
pnpm --filter @ats/web test
pnpm --filter @ats/api test
```

### Типы тестов

- **Unit тесты** — для утилит, хуков, компонентов
- **Integration тесты** — для API endpoints, сервисов
- **E2E тесты** — для критичных user flows
- **Component тесты** — Storybook interaction tests

## 🔌 MCP Integration (Model Context Protocol)

Проект поддерживает интеграцию с Figma через MCP для автоматической генерации компонентов на основе дизайнов.

### Быстрая настройка Figma MCP

```bash
# Запустить скрипт настройки
./scripts/setup-figma-mcp.sh

# Или вручную через CLI
claude mcp add figma --scope user --url https://mcp.figma.com/mcp

# Проверить установку
claude mcp list
```

### Возможности с Figma MCP

- 📐 **Прямой доступ к дизайнам** — чтение компонентов, токенов дизайна, текста
- 🎨 **Автогенерация компонентов** — создание React компонентов на основе Figma
- ✅ **Проверка соответствия** — сравнение кода с дизайном
- 🖼️ **Экспорт ресурсов** — автоматический экспорт изображений и иконок

### Подробная документация

См. [MCP Setup Guide](./docs/mcp-setup.md) для:
- Пошаговой инструкции по настройке
- Конфигурации нескольких MCP серверов
- Troubleshooting и решения проблем
- Примеры использования

## 📚 Структура базы данных (Supabase)

### Основные таблицы

```sql
-- Пользователи (расширение Supabase Auth)
users (
  id uuid PRIMARY KEY,
  email text UNIQUE,
  created_at timestamp,
  status text (active, blocked)
)

-- Приглашения
invitations (
  id uuid PRIMARY KEY,
  key text UNIQUE,
  email text,
  created_by uuid REFERENCES users(id),
  used_by uuid REFERENCES users(id),
  status text (active, used, revoked),
  created_at timestamp,
  used_at timestamp
)

-- WireGuard конфигурации
wireguard_configs (
  id uuid PRIMARY KEY,
  user_id uuid REFERENCES users(id),
  device_name text,
  public_key text,
  private_key text,
  preshared_key text,
  server_ip text,
  server_port integer,
  peer_ip text,
  created_at timestamp,
  updated_at timestamp
)

-- Proxy настройки
proxy_configs (
  id uuid PRIMARY KEY,
  wireguard_config_id uuid REFERENCES wireguard_configs(id),
  username text,
  password text,
  host text,
  port integer,
  created_at timestamp,
  updated_at timestamp
)

-- Статистика подключений
connection_stats (
  id uuid PRIMARY KEY,
  wireguard_config_id uuid REFERENCES wireguard_configs(id),
  timestamp timestamp,
  status text (connected, disconnected),
  bytes_sent bigint,
  bytes_received bigint,
  speed_mbps decimal
)

-- Audit log
audit_log (
  id uuid PRIMARY KEY,
  user_id uuid REFERENCES users(id),
  admin_id uuid REFERENCES users(id), -- для имперсонации
  action text,
  entity_type text,
  entity_id uuid,
  details jsonb,
  created_at timestamp
)
```

## 🔐 Безопасность

### Реализованные меры безопасности

- ✅ JWT аутентификация
- ✅ Row Level Security (RLS) в Supabase
- ✅ Хеширование паролей (bcrypt)
- ✅ Rate limiting на API endpoints
- ✅ CORS конфигурация
- ✅ Input валидация (Zod)
- ✅ Защита от SQL injection (Prisma ORM)
- ✅ Защита от XSS (React escaping)
- ✅ Secure cookie настройки
- ✅ Audit logging всех критичных операций
- ✅ Безопасное хранение WireGuard ключей

## 🐳 Docker Configuration

Пример текущей конфигурации для одного клиента:

```yaml
services:
  ats-wireguard:
    image: sgaluza/ats-wg:0.6
    container_name: ats-wireguard-{client-name}
    ports:
      - "{unique-http-port}:8080"
      - "{unique-wg-port}:51820/udp"
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
    environment:
      - WG_PRIVATE_KEY=${WG_PRIVATE_KEY}
      - WG_PEER_PUBLIC_KEY=${WG_PEER_PUBLIC_KEY}
      - WG_PEER_PRESHARED_KEY=${WG_PEER_PRESHARED_KEY}
      - WG_LOCAL_ADDRESS=10.7.7.1/32
      - WG_PEER_ADDRESS=10.7.7.2/32
      - PROXY_AUTH=${PROXY_AUTH}
    restart: unless-stopped
```

В self-service версии эта конфигурация будет создаваться **автоматически** для каждого нового подключения пользователя.

## 📖 Документация

- [API Documentation](./docs/api.md)
- [Frontend Architecture](./docs/frontend.md)
- [Backend Architecture](./docs/backend.md)
- [Database Schema](./docs/database.md)
- [Deployment Guide](./docs/deployment.md)
- [Contributing Guide](./CONTRIBUTING.md)

## 🗺️ Roadmap

### Phase 1: MVP (Current)
- [x] Базовая структура проекта
- [ ] Регистрация и аутентификация
- [ ] Генерация WireGuard конфигураций
- [ ] Базовый dashboard пользователя
- [ ] Админ-панель

### Phase 2: Core Features
- [ ] Автоматическое создание Docker контейнеров для клиентов
- [ ] Мониторинг и статистика в реальном времени
- [ ] Email notifications
- [ ] Гайды по настройке роутеров

### Phase 3: Advanced Features
- [ ] Множественные серверные локации
- [ ] API для программного управления
- [ ] Billing и подписки
- [ ] Mobile приложение
- [ ] Webhooks

## 🤝 Contributing

См. [CONTRIBUTING.md](./CONTRIBUTING.md) для деталей о процессе разработки.

## 📝 Лицензия

Proprietary - All rights reserved

## 👥 Команда

- Backend & Infrastructure: [Your Name]
- Frontend & UI/UX: [Your Name]
- DevOps: [Your Name]

## 📞 Контакты

- Email: support@ats-service.com
- Документация: https://docs.ats-service.com
- Support: https://support.ats-service.com

---

Made with ❤️ by ATS Team
